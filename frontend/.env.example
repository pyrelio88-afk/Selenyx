# The installed desktop app supplies its own loopback API endpoint. Do not put
# provider keys, secrets, or mobile companion addresses in VITE_* variables:
# Vite embeds them into the client bundle.
VITE_API_BASE_URL=
